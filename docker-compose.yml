networks:
  medilabosolutions-net:
    driver: bridge

volumes:
  mysql-data:
  mongo-data:

services:
  mysql:
    image: mysql:8
    environment:
      MYSQL_ROOT_PASSWORD: rootroot
      MYSQL_DATABASE: medilabosolutions_patient
    volumes:
      - ./patient/data:/docker-entrypoint-initdb.d:ro
      - mysql-data:/var/lib/mysql
    healthcheck:
      test: ["CMD", "mysqladmin", "ping", "-h", "localhost"]
      interval: 10s
      timeout: 5s
      retries: 5
    networks:
      - medilabosolutions-net

  mongo:
    image: mongo:6
    environment:
      MONGO_INITDB_ROOT_USERNAME: ${MONGO_ROOT}
      MONGO_INITDB_ROOT_PASSWORD: ${MONGO_PASS}
    volumes:
      - ./note/data:/docker-entrypoint-initdb.d:ro
      - mongo-data:/data/db
    healthcheck:
      test: ["CMD", "mongosh", "--eval", "db.adminCommand('ping')"]
      interval: 10s
      timeout: 5s
      retries: 5
    networks:
      - medilabosolutions-net

  eureka:
    image: ${REGISTRY}/${IMAGE_OWNER}/medilabo-eureka:${TAG:-latest}
    ports:
      - "8761:8761"
    healthcheck:
      test: ["CMD", "true"]
      interval: 10s
      timeout: 5s
      retries: 5
    networks:
      - medilabosolutions-net

  patient:
    image: ${REGISTRY}/${IMAGE_OWNER}/medilabo-patient:${TAG:-latest}
    ports:
      - "8081:8081"
    environment:
      DB_URL: jdbc:mysql://mysql:3306/medilabosolutions_patient
      DB_USERNAME: ${MYSQL_USER}
      DB_PASSWORD: ${MYSQL_PASSWORD}
      EUREKA_CLIENT_SERVICE_URL_DEFAULT_ZONE: http://eureka:8761/eureka/
    depends_on:
      mysql:
        condition: service_healthy
      eureka:
        condition: service_healthy
    healthcheck:
      test: ["CMD", "true"] #["CMD", "curl", "-f", "http://localhost:8100/actuator/health"]
      interval: 10s
      timeout: 5s
      retries: 5
    networks:
      - medilabosolutions-net

  note:
    image: ${REGISTRY}/${IMAGE_OWNER}/medilabo-note:${TAG:-latest}
    ports:
      - "8082:8082"
    environment:
      DB_URI: mongodb://${MONGO_ROOT}:${MONGO_PASS}@mongo:27017/medilabosolutions_note?authSource=admin
      EUREKA_CLIENT_SERVICE_URL_DEFAULT_ZONE: http://eureka:8761/eureka/
      JWT_SECRET: ${JWT_SECRET}
    depends_on:
      mongo:
        condition: service_healthy
      eureka:
        condition: service_healthy
    healthcheck:
      test: ["CMD", "true"] # ["CMD", "curl", "-f", "http://localhost:8090/actuator/health"]
      interval: 10s
      timeout: 5s
      retries: 5
    networks:
      - medilabosolutions-net

  assessment:
    image: ${REGISTRY}/${IMAGE_OWNER}/medilabo-assessment:${TAG:-latest}
    ports:
      - "8083:8083"
    environment:
      EUREKA_CLIENT_SERVICE_URL_DEFAULT_ZONE: http://eureka:8761/eureka/
    depends_on:
      eureka:
        condition: service_healthy
    healthcheck:
      test: ["CMD", "true"] # ["CMD", "curl", "-f", "http://localhost:8080/actuator/health"]
      interval: 10s
      timeout: 5s
      retries: 5
    networks:
      - medilabosolutions-net

  gateway:
    image: ${REGISTRY}/${IMAGE_OWNER}/medilabo-gateway:${TAG:-latest}
    ports:
      - "8080:8080"
    environment:
      EUREKA_CLIENT_SERVICE_URL_DEFAULT_ZONE: http://eureka:8761/eureka/
      JWT_SECRET: ${JWT_SECRET}
    depends_on:
      assessment:
        condition: service_healthy
      patient:
        condition: service_healthy
      note:
        condition: service_healthy
      eureka:
        condition: service_healthy
    healthcheck:
      test: ["CMD", "true"] # ["CMD", "curl", "-f", "http://localhost:8080/actuator/health"]
      interval: 10s
      timeout: 5s
      retries: 5
    networks:
      - medilabosolutions-net

  front:
    image: ${REGISTRY}/${IMAGE_OWNER}/medilabo-front:${TAG:-latest}
    ports:
      - "8084:8084"
    environment:
      JWT_SECRET: ${JWT_SECRET}
      JWT_EXPIRATION: "${JWT_EXPIRATION:-86400000}"
      GATEWAY_URL: http://gateway:8080
    depends_on:
      gateway:
        condition: service_healthy
    healthcheck:
      test: ["CMD", "true"] # ["CMD", "curl", "-f", "http://localhost:8081/actuator/health"]
      interval: 10s
      timeout: 5s
      retries: 5
    networks:
      - medilabosolutions-net
